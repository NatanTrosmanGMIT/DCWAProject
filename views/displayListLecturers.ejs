<style>
    th {
      font-size: 14px;
      text-transform: uppercase;
      letter-spacing: 0.03em;
      box-shadow: 2px 2px 2px 2px rgba(0,0,0,0.1);
      border: 1px solid black;
      text-align: center;
    }
  
     td {
        padding: 10px 10px;
        color: #2b2b2b;
          content: attr(data-label);
          flex-basis: 50%;
          text-align: right;
          border: 1px solid black;
          box-shadow: 2px 2px 2px 2px rgba(0,0,0,0.1);
          text-align: center;
      }
    </style>
  <table>
      <tr>
          <th>Lecturer ID</th>
          <th>Name</th>
          <th>Dept</th>
          <th>Action</th>
      </tr>
      <% lecturer.forEach((lecturers)=> { %>
          <tr>
              <td><%= lecturer._id %> </td>
              <td><%= lecturer.name %></td>
              <td><%= lecturer.dept %></td>
              <td><a href="/addLecturer">Add Lecturer</a></td>
          </tr>
          <% }) %>
  </table>
  <br/>
  <a href="/">Home</a>
  